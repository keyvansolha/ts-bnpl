# تاریخچه تغییرات / Changelog

همه‌ی تغییرات قابل‌توجه این پروژه در این فایل ثبت می‌شوند.

---

## [0.5.2] — 2026-09-05

### برطرف شده / Fixed

- شماره‌ی هر «قدم خرید» حالا فرزند خودِ دایره‌ی آیکون است و روی لبه‌ی آن
  می‌نشیند؛ پیش از این با مختصات مطلق نسبت به کل ستون جدا از دایره بالای خط
  راهنما شناور بود. خط اتصال دسکتاپ هم به مرکز عمودی دایره (۵۲px) منتقل شد.

### تغییر کرده / Changed

- **استایل بنر بالای صفحه دقیقاً با بنر هوم‌پیج یکی شد.** فلش‌ها و نقطه‌ها حالا در
  همان قرص `.wbs-slider-controls` پایینِ وسط بنر (با همان بریدگی‌های گوشه، ارتفاع
  ۴۵px و رنگ `--body-bg`) قرار می‌گیرند؛ پیش از این دکمه‌های دایره‌ای سرمه‌ای کنار
  بنر و نقطه‌های سبز جدا بودند. کادر بنر هم `border` و `box-shadow` اضافی را از
  دست داد و کف ارتفاع ۴۰۰/۳۶۰/۲۵۲ پیکسل هوم‌پیج را گرفت. کنترل‌ها دیگر در موبایل
  مخفی نمی‌شوند و شعاع گوشه در همه‌ی اندازه‌ها ۲۰px می‌ماند.
- چون `home.css` فقط در صفحه‌ی نخست بارگذاری می‌شود، این قواعد در استایل خود افزونه
  آینه شده‌اند و آزمون `visual-css` آن‌ها را مستقیماً با فایل قالب مقایسه می‌کند تا
  هر واگرایی بعدی fail بدهد.
- **فراخوان پایانی به بنر تبدیل شد.** با بارگذاری تصویر، این بخش دقیقاً مثل بنر
  بالای صفحه یک تصویر تمام‌عرض لینک‌دار است (`1326:400` دسکتاپ، `4:3` زیر ۶۵۰px)
  و ستون سرمه‌ای خالیِ کنار تصویر حذف شده است؛ عنوان، توضیح و دکمه باید داخل خود
  تصویر طراحی شوند. بدون تصویر، همان کارت متنی قبلی با دکمه باقی می‌ماند.
- نشانی فراخوان پایانی مقصد کلیک کل بنر است و عنوان بخش، نام دسترس‌پذیر لینک و
  `alt` تصویر را در نبود alt ذخیره‌شده تأمین می‌کند.
- راهنمای تصویر و پنل پیشخوان «Final CTA» با ابعاد و قواعد جدید بنر به‌روز شدند.

---

## [0.5.1] — 2026-09-03

### برطرف شده / Fixed

- track بنر تصویری دوباره `flex` است تا اسلایدهای Swiper افقی حرکت کنند؛
  autoplay، فلش‌ها و pagination دیگر تصویر اشتباه نشان نمی‌دهند.
- مقصدهای «مراحل خرید» و «محصولات» در دسکتاپ و موبایل از زیر هدر sticky
  بیرون می‌آیند.
- سرویس‌های اقساطی ثبت‌شده و فعال برای معرفی عمومی به وضعیت لحظه‌ای سبد خرید
  وابسته نیستند؛ در دسترس‌بودن نهایی همچنان در checkout کنترل می‌شود و حالت
  آزمایشی دیجی‌پی عمومی نمی‌شود.
- نمونه‌ی تشخیص کالای واجد شرایط از همان تیزر واقعی صفحه محصول، شیت اصلی آن و
  مقصد داخلی فهرست محصولات استفاده می‌کند.
- CTA پایانی در دسکتاپ به نسبت ۶۰/۴۰ و ارتفاع ۳۶۰ تا ۴۲۰ پیکسل محدود شد؛ crop
  موبایل ۴:۳ و راهنمای تصویر افقی محصول‌محور نیز به‌روز شد.
- بنر لینک‌دار بدون alt ذخیره‌شده، fallback متنی امن و قابل‌فهم می‌گیرد.

---

## [0.5.0] — 2026-09-02

### اضافه شده / Added

- **حالت چهارم `Visual Landing (Experimental)`** که روی همان برگه‌ی انتخابی
  لندینگ فعال می‌شود، محتوای ویرایشگر را فقط در این حالت جایگزین می‌کند و
  حالت قدیمی Landing را بدون تغییر نگه می‌دارد.
- **مدیر محتوای اختصاصی** در «محصولات ← لندینگ تصویری (آزمایشی)» برای بنرهای
  مرتب‌شونده، هیرو، سرویس‌های اعتباری، تشخیص کالا، شرایط و فراخوان پایانی.
- **تصاویر واکنش‌گرای کنترل‌شده** با source جدا برای دسکتاپ/موبایل و
  AVIF/WebP، ابعاد صریح، alt و اولویت LCP برای اولین بنر.
- **سرویس‌های عملیاتی**: فقط درگاه اقساطی ثبت‌شده، فعال، قابل استفاده و خارج
  از حالت آزمایشی در صفحه‌ی عمومی دیده می‌شود؛ دیجی‌پی fallback لوگوی SVG دارد.
- **رندر یکپارچه با قالب**: کارت محصول، منطق `_bnpl_price`، کاروسل، FAQ، لوگوی
  متحرک و توکن‌های رنگی موجود دوباره استفاده شده‌اند.
- **رابط RTL و دسترس‌پذیر** با اسلایدر شرطی، توقف روی hover/focus/hidden، احترام
  به `prefers-reduced-motion` و focus state قابل مشاهده.
- راهنمای دقیق خروجی تصاویر در
  `docs/bnpl-visual-landing-image-specs.md` و آزمون‌های رفتاری PHP/JavaScript.

### ایزوله‌سازی / Isolation

- تنظیمات حالت تصویری در option مستقل `ts_bnpl_visual_landing_settings` ذخیره
  می‌شوند؛ checkout، محاسبات، گزارش و داده‌های قیمت اقساطی تغییر نکرده‌اند.
- breadcrumb و عنوان برگه فقط در حالت تصویری مخفی می‌شوند. سایر صفحات و حالت
  Landing قدیمی همان رفتار و assetهای قبلی را دارند.

---

## [0.4.3] — 2026-08-31

### برطرف شده / Fixed

هر دو باگ گزارش‌شده یک ریشه داشتند: **لایه‌ی «متن ویرایشگر» قالب**.

قالب در `assets/scss/components/_content-global.scss` هرچه داخل `.page-content`
باشد را متنِ نوشته‌شده در ویرایشگر فرض می‌کند و در `page.css` با مشخصه‌ی
`(0,2,2)` و جاهایی با `!important` به `a`، `img`، `h2..h6` و `p` دست می‌زند.
لندینگ از `the_content` تزریق می‌شود، یعنی دقیقاً وسط همان قانون‌ها می‌افتد.
نتیجه‌اش:

- **زیرخط و تغییر رنگ دکمه‌ها در هاور.** `.page-content a` زیرخط و
  `color: var(--secondary)` می‌داد و `a:hover` رنگ را لایم می‌کرد. در حالت
  تاریک `--secondary` خودش لایم است، پس متنِ دکمه هم‌رنگ پس‌زمینه‌ی دکمه
  می‌شد — همان «بد لود شدن روی دارک مود».
- **خط زیر کارت محصول.** عنوان کارت داخل یک `<a>` بلوکی با `min-height: 80px`
  است، پس همان زیرخط به شکل یک خط زیر کارت دیده می‌شد.
- **به‌هم‌ریختن کاروسل روی دسکتاپ.** `.page-content img` عکس کارت‌ها را تا عرض
  کامل کارت کش می‌داد (حس «زوم») و با `!important` سی پیکسل مارجین عمودی
  می‌گذاشت، و `h2..h6` عنوان کارت‌ها را ۱۸ پیکسل با مارجین ۳۰ پیکسلی می‌کرد.
  چون این ارتفاع‌ها به محتوای هر کارت بستگی داشت، کارت‌ها بالا و پایین
  می‌افتادند. اینکه فقط دسکتاپ خراب بود هم از همین‌جاست: `pageMobile.scss`
  این لایه را اصلاً وارد نمی‌کند.

اصلاح: افزونه روی همان یک برگه کلاس `ts-bnpl-landing-page` را به `body`
اضافه می‌کند و شیت لندینگ آن لایه را با انتخابگر سه‌کلاسه — یعنی مشخصه‌ی
بالاتر از `(0,2,2)` — خنثی می‌کند. اندازه‌ی عنوان‌ها و رنگ و وزن دکمه‌ها از
توکن‌های خود قالب می‌آیند و هیچ رنگ تازه‌ای ساخته نشده. رنگ دکمه‌ها در هر چهار
حالت (عادی، هاور، فوکوس، فعال) یکی است.

- **دکمه‌های ناوبری کاروسل کار افتادند.** مسیر `Page` قالب هندل `swiper` را
  به بیلد **هسته** (`assets/plugins/swiper/swiper.min.js`) وصل می‌کند که فقط
  `Resize` و `Observer` را نصب می‌کند و ماژول `Navigation` ندارد. شرطِ
  «اگر صف نشده صف کن» در نسخه‌ی قبل هرگز اجرا نمی‌شد، چون هندل از قبل در صف
  بود؛ یعنی اصلاحِ ۰.۴.۱ در عمل هیچ‌وقت اثر نکرد. حالا خودِ هندل روی
  `swiper-bundle` نشانده می‌شود، همان بیلدی که صفحه‌ی محصول با همین اسکریپت
  کاروسل می‌دهد.
- **ترتیب چاپ شیت‌ها قطعی شد.** شیت لندینگ حالا به شیت‌های قالبِ همین صفحه
  وابسته است، پس همیشه بعد از آن‌ها چاپ می‌شود.
- هندل CSS ماژول کاروسل به `products-carousel` تغییر کرد، همان اسمی که مسیر
  محصول و مسیر ۴۰۴ قالب برای همین فایل به کار می‌برند؛ قبلاً دو اسم بود و
  فایل می‌توانست دو بار بیاید.

---

## [0.4.2] — 2026-08-31

### برطرف شده / Fixed
- **فاصله‌ی دوبرابر اسلایدها روی دسکتاپ.** CSS ماژول قالب به هر اسلاید
  `margin-left: 20px` می‌دهد و سوایپر هم برای `spaceBetween` مارجین اینلاین
  می‌نویسد؛ این دو روی هم جمع می‌شدند و کارت‌ها جا نمی‌گرفتند.
- **کارت‌ها هم‌قد شدند.** در آرشیو گرید آن‌ها را کش می‌آورد؛ در کاروسل باید
  صریح خواسته می‌شد، وگرنه هر کارت به اندازه‌ی محتوای خودش کوتاه و بلند می‌شد.
- **دکمه‌ها دقیقاً همان `.wbs-button` سایت شدند** — قرصی، لایم روی سرمه‌ای، با
  کلاس `button-no-hover` خود قالب. هیچ رنگ، هاور یا زیرخطی در افزونه تعریف
  نمی‌شود.
- زیرخط لینک‌ها در کل لندینگ خنثی شد.

---

## [0.4.1] — 2026-08-31

### برطرف شده / Fixed
- **کاروسل روی دسکتاپ درست لود می‌شود.** افزونه تنظیمات سوایپر را خودش تکرار
  می‌کرد و `slidesPerView` و `spaceBetween` می‌داد، در حالی که CSS ماژول قالب
  از قبل عرض و مارجین اسلاید را می‌دهد؛ مارجین اینلاین سوایپر روی مارجین CSS
  سوار می‌شد. حالا همان اسکریپت `modular/product-carousel.min.js` قالب
  راه‌اندازی‌اش می‌کند و اسکریپت اختصاصی افزونه حذف شد.
- **بیلد درست سوایپر.** هسته‌ی خالی لود می‌شد که ماژول Navigation ندارد؛ حالا
  `swiper-bundle` است، همان چیزی که بقیه‌ی صفحات قالب با این اسکریپت می‌دهند.
- **انیمیشن هاور دکمه‌ها کاملاً برداشته شد.** هر تغییر پس‌زمینه‌ای در هاور
  خوانایی متن را کم می‌کرد. رنگ در همه‌ی حالت‌ها ثابت است.

---

## [0.4.0] — 2026-08-31

### اضافه شده / Added
- **صفحه‌ی فرود خرید اعتباری:** یک برگه‌ی معمولی وردپرس از تنظیمات انتخاب می‌شود
  و افزونه کل محتوا را رویش می‌سازد
- **حالت سوم نمایش:** تیزر صفحه‌ی محصول به لندینگ لینک می‌شود
- **کاروسل کالاهای واجد شرایط** با کارت و اسلایدر خود قالب
- **فیلتر آرشیو فروشگاه:** `?ts_bnpl=1` فروشگاه را به کالاهای اعتباری محدود
  می‌کند، با مرتب‌سازی و صفحه‌بندی همیشگی

### برطرف شده / Fixed
- **نسخه دیگر از هدر افزونه جدا نمی‌شود.** `TS_BNPL_VERSION` روی `0.1.0` مانده
  بود در حالی که هدر `0.3.1` بود؛ یعنی cache-busting همه‌ی CSS و JS کار
  نمی‌کرد و مرورگرها فایل قدیمی را نگه می‌داشتند. حالا مستقیم از هدر خوانده
  می‌شود.
- کارت‌های کاروسل روی موبایل روی هم می‌افتادند؛ قالب اشتباهی لود می‌شد
- استایل کارت و کاروسل روی برگه‌ی ساده اصلاً صف نمی‌شد (متن سبز در حالت تاریک)
- هاور دکمه‌ها پس‌زمینه را تیره می‌کرد و متن را محو

---

## [0.1.1] — 2026-08-29

### اضافه شده / Added
- **تیزر ثانویه برای خرید اعتباری:** متن "امکان خرید ۴ قسطه این کالا از تهران‌اسپیکر" جایگزین کارت کامل شد
- **دو حالت نمایش:**
  - **بازشونده (Accordion):** جزئیات همان‌جا باز می‌شود
  - **پنل راهنما (Modal):** تیزر پنل راهنما را باز می‌کند
- **تنظیمات قابل‌ویرایش:** حالت نمایش و متن تیزر در پیشخوان
- **بازمحاسبه‌ی قیمت چک‌اوت:** هنگام تعویض روش پرداخت
- **لوگوی دیجی‌پی:** جایگزین آیکن تقویم
- **دکمه کمک مرکزی:** SVG «؟» برای بهتر کردن مرکز‌یابی
- **نقشه در سفارش پیشخوان:** نمایش محل تحویل با iframe OpenStreetMap
- **نقشه داخل فیلد آدرس:** انتخاب نقشه اکنون بخشی از "نشانی تحویل" است
- **بند هشدار قرمز:** موقع انتخاب اقساط، خطر تغییر قیمت واضح‌تر است

### تغییر شده / Changed
- هر تیزر تا کاربر کلیک نکند، مبلغ قسط و نام ارائه‌دهنده رندر نمی‌کند
- قیمت نقدی و دکمه خرید همچنان CTA اصلی صفحه می‌مانند
- فاصله‌ی مودال و مرتب‌سازی متن بهتر شد

### برطرف شده / Fixed
- قیمت اقساطی در چک‌اوت فقط با تغییر آدرس اعمال می‌شد
- ردیف اقساط روی موبایل فشرده می‌شد
- نقشه انتخاب آدرس یک ردیف جدا اشغال می‌کرد

---

## [0.1.0] — 2026-08-27

### اضافه شده / Added
- **نمایش قیمت اقساطی:** بر روی صفحه‌ی محصول و متغیرهای آن
- **گزارش قیمت‌های اقساطی:** جدول تمام محصولات با اختلاف قیمت
- **مرتب‌سازی و فیلتر:** براساس درصد تفاوت
- **خروجی CSV:** همراهی با سیستم حسابداری
- **پنل راهنما:** ۵ مرحله‌ی خرید اقساطی و شروط
- **مودال فوکوس‌گرفت:** کلیدهای Tab و Escape کار می‌کنند
- **سازگاری HPOS:** با جدول سفارش‌های سفارشی ووکامرس
- **فیلد تنظیم ریاضی:** دکمه‌ی راهنما و متن توضیح در پیشخوان
- **ادغام درگاه دیجی‌پی:** نمایش فقط اگر درگاه فعال و ارائه‌دهنده قیمت دارد
- **مسدود کردن کیف پول:** هنگام استفاده‌ی اقساط
- **حالت آزمایشی:** برای تست روی staging، تیک در تنظیمات دیجی‌پی

---

## سیاست نسخه‌بندی / Versioning

این پروژه از [Semantic Versioning](https://semver.org/) پیروی می‌کند:
- **MAJOR:** تغییر اساسی در رفتار یا واجهه
- **MINOR:** ویژگی جدید بدون شکستن سازگاری
- **PATCH:** رفع اشکال

---

## فرمت / Format

الهام‌گرفته از [Keep a Changelog](https://keepachangelog.com/).
