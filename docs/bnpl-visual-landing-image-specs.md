# مشخصات تحویل تصویر لندینگ تصویری BNPL

این سند مرجع نهایی طراح و مدیر سایت برای خروجی‌گرفتن، نام‌گذاری و بارگذاری
تصاویر حالت **Visual Landing (Experimental)** است. اندازه‌ها اندازه‌ی فایل
خروجی‌اند، نه صرفاً نسبت پیشنهادی در ماکاپ.

## جدول تحویل

| بخش / دارایی | کاربرد | دسکتاپ | نسبت دسکتاپ | موبایل | نسبت موبایل | فرمت لازم | الزام | حجم پیشنهادی / سقف هر فایل | برش و `object-position` | محدوده‌ی امن | متن داخل تصویر | بارگذاری | محل بارگذاری در پیشخوان |
|---|---|---:|---:|---:|---:|---|---|---:|---|---|---|---|---|
| Top Banner — هر اسلاید | بنر تمام‌عرض بالای بدنه | `1326×400 px` | `3.315:1` | `1300×975 px` | `4:3` | AVIF + WebP | خود بخش اختیاری؛ برای هر اسلاید چهار فایل لازم است | `180 KB` / `300 KB` | `cover`؛ سوژه‌ی اصلی را نزدیک مرکز نگه دارید | هیچ عنصر مهمی در ۸٪ حاشیه‌ها نباشد؛ محتوای حیاتی در ۸۰٪ عرض و ۷۰٪ ارتفاع دسکتاپ و ۸۰٪ عرض و ۷۵٪ ارتفاع موبایل | مجاز، چون روی بنر HTML قرار نمی‌گیرد؛ کوتاه و خوانا باشد | اسلاید اول `eager` و `high` (LCP)؛ بقیه `lazy` | محصولات ← لندینگ تصویری (آزمایشی) ← **Top Banners** |
| Hero visual | تصویر کنار معرفی اصلی | `1000×1000 px` | `1:1` | `1080×1080 px` | `1:1` | AVIF + WebP | اختیاری؛ در نبود تصویر چیدمان متنی سالم می‌ماند | `140 KB` / `220 KB` | `contain`، مرکز | ۱۰٪ فاصله‌ی امن از هر چهار لبه | ممنوع؛ همه‌ی متن‌ها HTML هستند | `lazy`؛ پایین بنرها | همان صفحه ← **Hero** |
| Eligibility visual | کمک تصویری برای تشخیص کالای واجد شرایط | `1000×1000 px` | `1:1` | `1080×1080 px` | `1:1` | AVIF + WebP | اختیاری | `140 KB` / `220 KB` | `contain`، مرکز | ۱۰٪ فاصله‌ی امن | ممنوع؛ توضیح بخش HTML است | `lazy`؛ زیر محصولات | همان صفحه ← **Eligibility** |
| Conditions visual | تصویر بخش مبلغ نهایی و شرایط | `1000×1000 px` | `1:1` | `1080×1080 px` | `1:1` | AVIF + WebP | اختیاری | `140 KB` / `220 KB` | `contain`، مرکز | ۱۰٪ فاصله‌ی امن | ممنوع؛ اطلاعات مالی فقط HTML است | `lazy`؛ پایین صفحه | همان صفحه ← **Conditions** |
| Final CTA visual | تصویر فراخوان پایانی | `1000×1000 px` | `1:1` | `1080×1080 px` | `1:1` | AVIF + WebP | اختیاری | `140 KB` / `220 KB` | `cover`، مرکز | سوژه در ۷۵٪ میانی بماند تا برش موبایل امن باشد | ممنوع؛ عنوان و دکمه HTML هستند | `lazy`؛ پایین صفحه | همان صفحه ← **Final CTA** |
| Provider logo | لوگوی کارت سرویس اعتباری | `512×512 px` بوم شفاف | `1:1` | همان فایل | `1:1` | PNG یا WebP؛ JPEG فقط برای فایل بدون شفافیت | اختیاری؛ دیجی‌پی SVG داخلی دارد | `25 KB` / `40 KB` | `contain`، مرکز | حداقل ۱۲٪ فضای خالی دور لوگو | فقط خود نشان رسمی | `lazy` | همان صفحه ← **Providers** ← لوگو |

## قواعد فرمت و fallback

- برای هر تصویر واکنش‌گرا چهار خروجی جدا تحویل دهید: دسکتاپ AVIF، دسکتاپ
  WebP، موبایل AVIF و موبایل WebP. WebP fallback الزامی است؛ AVIF برای مرورگرهای
  پشتیبان اولویت دارد.
- فایل JPEG/PNG جایگزین چهار خروجی واکنش‌گرا نمی‌شود. PNG/JPEG فقط در فیلد
  اختصاصی لوگوی سرویس پذیرفته می‌شود.
- کادر AVIF و WebP یک variant باید دقیقاً یکسان باشد. رنگ، جهت سوژه و محل
  عناصر میان دو فرمت تغییر نکند.
- موبایل یک crop مستقل است؛ تصویر دسکتاپ را صرفاً فشرده یا باریک نکنید.
- پروفایل رنگی `sRGB`، متادیتای غیرضروری حذف و sharpening پس از resize انجام شود.

## نام‌گذاری فایل‌ها

از حروف کوچک انگلیسی، خط تیره و شماره‌ی دو رقمی استفاده کنید:

```text
bnpl-top-banner-01-desktop.avif
bnpl-top-banner-01-desktop.webp
bnpl-top-banner-01-mobile.avif
bnpl-top-banner-01-mobile.webp

bnpl-hero-desktop.avif
bnpl-hero-desktop.webp
bnpl-hero-mobile.avif
bnpl-hero-mobile.webp

bnpl-eligibility-desktop.avif
bnpl-eligibility-desktop.webp
bnpl-eligibility-mobile.avif
bnpl-eligibility-mobile.webp

bnpl-conditions-desktop.avif
bnpl-conditions-desktop.webp
bnpl-conditions-mobile.avif
bnpl-conditions-mobile.webp

bnpl-final-cta-desktop.avif
bnpl-final-cta-desktop.webp
bnpl-final-cta-mobile.avif
bnpl-final-cta-mobile.webp

bnpl-provider-digipay-logo.webp
```

## نکات بارگذاری و دسترس‌پذیری

1. ابتدا همه‌ی فایل‌ها را در کتابخانه‌ی رسانه‌ی وردپرس بارگذاری کنید، سپس در
   کارت بخش مربوطه چهار فیلد رسانه را پر کنید.
2. برای هر گروه واکنش‌گرا متن جایگزین فارسی و کوتاه بنویسید. اگر تصویر کاملاً
   تزئینی است، فیلد alt را خالی بگذارید؛ متن روی خود تصویر را در alt تکرار نکنید.
3. ترتیب بنرها در پیشخوان همان ترتیب نمایش عمومی است. اولین بنر را بهینه‌ترین
   فایل قرار دهید، چون کاندید LCP و تنها تصویر با اولویت بالا است.
4. یک بنر بدون کنترل اسلایدر نمایش داده می‌شود. از دو بنر به بالا کنترل‌ها فعال
   می‌شوند و در حالت «کاهش حرکت» autoplay خاموش است.
5. اگر variant نامعتبر یا حذف شود، renderer از source معتبر بعدی استفاده
   می‌کند؛ اما یک WebP معتبر برای ساخت `<img>` fallback لازم است. بخش‌های
   تصویری اختیاری بدون تصویر از صفحه حذف یا به چیدمان متنی تبدیل می‌شوند.
6. لوگوی دیجی‌پی به‌صورت SVG داخل افزونه موجود است. فقط وقتی فایل رسمی تازه‌ای
   دارید آن را با PNG/WebP شفاف جایگزین کنید.

